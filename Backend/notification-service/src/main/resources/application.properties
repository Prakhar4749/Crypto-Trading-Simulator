spring.config.import=optional:file:../.env[.properties]
server.port=${NOTIFICATION_PORT:5460}

spring.application.name=notification-service

spring.mail.host=${SMTP_HOST:smtp.sendgrid.net}
spring.mail.port=${SMTP_PORT:2525}
spring.mail.username=${SMTP_USERNAME:changeme}
spring.mail.password=${SMTP_PASSWORD:changeme}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=notification-service-group-v2
spring.kafka.consumer.auto-offset-reset=latest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka}
internal.service.api.key=${INTERNAL_SERVICE_API_KEY:changeme}
notification.otp-expiry=${OTP_EXPIRY_MINUTES:10}

app.name=${APP_NAME:CoinDesk}
app.logo-url=${LOGO_URL:}
app.frontend-url=${FRONTEND_URL:http://localhost:5173}
# Kafka Topics
kafka.topic.otp-notification=${KAFKA_TOPIC_OTP_NOTIFICATION:otp-notification}
kafka.topic.user-created=${KAFKA_TOPIC_USER_CREATED:user-created}
kafka.topic.trade-executed=${KAFKA_TOPIC_TRADE_EXECUTED:trade-executed}
kafka.topic.withdrawal-status=${KAFKA_TOPIC_WITHDRAWAL_STATUS:withdrawal-status}

logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.prakhar=${LOG_LEVEL:INFO}

# DB Exclude
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\
  org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
